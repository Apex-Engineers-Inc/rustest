/target
.claude/settings.local.json
__pycache__/
*.pyc
*.pyo
*.pyd
.env
.venv
/build
/dist
*.so
*.dylib
*.dll
.mypy_cache/
.pytest_cache/
.rustest_cache/
# `rustest --cov` writes coverage.py's own data file here, and the parallel-mode files
# a worker writes before they are combined.
.coverage
.coverage.*
coverage.xml
.DS_Store
/external
/rustest/
test-results.json
/site/
conformance/bench_results.json
# `python -m conformance --real` stages shallow clones of the OSS targets, their isolated
# venvs, the rustest wheels built for those interpreters, and per-run JSON reports here.
# Everything in it is reproducible from conformance/real/*.toml.
conformance/real/_work/

# Demo recordings (generated by VHS)
